{
  "name": "@forge/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "bun": ">=1.2.22" },
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch src/server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@forge/shared": "workspace:*",
    "@forge/wireup": "workspace:*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/bun": "^1.2.22",
    "typescript": "^7.0.2"
  }
}
